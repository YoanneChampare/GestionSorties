{% extends 'base.html.twig' %}
{% block body %}
    <div class="form">
        {{ form_start(loginForm) }}
            <div class="form-group">{{ form_row(loginForm.mail,{'attr':{'class':'form-control'}}) }}</div>
            <div class="form-group">{{ form_row(loginForm.mdp,{'attr':{'class':'form-control'}}) }}</div>
        {{ form_end(loginForm) }}
        <button type="submit" class="btn btn-primary">Connexion</button>
    </div>

{% endblock %}